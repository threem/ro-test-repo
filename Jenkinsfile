node() {
    stage('build'){
        echo "docker build"
        }
    stage('run & test'){
        echo "docker run & test something & docker stop"
        }
    stage('push to ECR'){
        echo "ECR push"
        }
    stage('NOTIFY'){
        echo "email & IM notify about status"
    }
}
